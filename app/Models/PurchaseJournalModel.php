<?php

namespace App\Models;

use CodeIgniter\Model;

class PurchaseJournalModel extends Model
{
    protected $DBGroup          = 'default';
    protected $table            = 'purchase_journals';
    protected $primaryKey       = 'id';
    protected $useAutoIncrement = true;
    protected $insertID         = 0;
    protected $returnType       = 'array';
    protected $useSoftDeletes   = false;
    protected $protectFields    = true;
    protected $allowedFields    = [
        "id", "purchase_transaction_id", "description", "ref", "journal_type", "total_price", "created_at", "updated_at"
    ];

    // Dates
    protected $useTimestamps = true;
    protected $dateFormat    = 'datetime';
    protected $createdField  = 'created_at';
    protected $updatedField  = 'updated_at';
    protected $deletedField  = 'deleted_at';

    // Validation
    protected $validationRules      = [];
    protected $validationMessages   = [];
    protected $skipValidation       = false;
    protected $cleanValidationRules = true;

    // Callbacks
    protected $allowCallbacks = true;
    protected $beforeInsert   = [];
    protected $afterInsert    = [];
    protected $beforeUpdate   = [];
    protected $afterUpdate    = [];
    protected $beforeFind     = [];
    protected $afterFind      = [];
    protected $beforeDelete   = [];
    protected $afterDelete    = [];

    public function getAll()
    {
        return $this->select('purchase_journals.*, purchase_transactions.trx_code, purchase_transactions.trx_date')
            ->join('purchase_transactions', 'purchase_transactions.id = purchase_journals.purchase_transaction_id')
            ->findAll();
    }
}
